services:
  gateway:
    build: .
    container_name: graphql_gateway
    ports:
      - "4000:4000"
    env_file:
      - .env
    volumes:
      - ./gateway:/app/gateway
    networks:
      - bookworm_network

networks:
  bookworm_network:
    external: true
    name: bookworm_network